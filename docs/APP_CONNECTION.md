# Application connection

Applications register with J OS and expose capabilities.

J Agent is the management/orchestration layer above them.

Example:

J Agent -> J OS -> J Verify
J Agent -> J OS -> J Tester
J Agent -> J OS -> J Security
J Agent -> J OS -> J Alert

The same model works in cloud and local deployments.
